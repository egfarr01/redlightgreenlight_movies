<!DOCTYPE html>
<html>
  <head>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Libre+Baskerville&family=Roboto+Slab&family=Rubik+Scribble&display=swap" rel="stylesheet">
    <%= stylesheet_link_tag "https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" %>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
    <link rel="apple-touch-icon" sizes="180x180" href="<%= asset_path('favicon_io/apple-touch-icon.png') %>">
    <link rel="icon" type="image/png" sizes="32x32" href="<%= asset_path('favicon_io/favicon-32x32.png') %>">
    <link rel="icon" type="image/png" sizes="16x16" href="<%= asset_path('favicon_io/favicon-16x16.png') %>">
    <link rel="manifest" href="/site.webmanifest">
    
    <title>RedlightgreenlightMovies</title>
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <%= csrf_meta_tags %> 
    <%= csp_meta_tag %> 
    <%= stylesheet_link_tag 'application', 'data-turbo-track': 'reload' %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav>
      <%if user_signed_in? %> 
        <%= link_to 'Home', root_path %> 
        <%= link_to 'Logout', destroy_user_session_path, method: :delete %>
        <%= link_to 'My RL/GL Movies', myRLGLM_path %>
      <%end %> 
      <% if current_user && current_user.admin? %> 
        <%= link_to "Add a movie", new_movie_path %> 
      <% end %>
      <h1 class="site-title">
        <span class="red">Red</span><span class="light">light</span><span class="green">Green</span><span class="light">light</span><span class="movies">Movies</span>
      </h1>
    </nav>
    <%= yield %>
  </body>
</html>
